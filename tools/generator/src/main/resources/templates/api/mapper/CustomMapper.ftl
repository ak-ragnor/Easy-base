package ${entity.componentPackage("api")}.model.mapper;

import ${entity.baseComponentPackage("api")}.model.mapper.${entity.name}BaseMapper;
import org.springframework.stereotype.Component;

/**
* Mapper for ${entity.name}.
* This class extends the base mapper and can be customized.
*/
@Component
public class ${entity.name}Mapper extends ${entity.name}BaseMapper {
// CUSTOM CODE START: methods
// Add custom mapping methods here
// CUSTOM CODE END: methods
}