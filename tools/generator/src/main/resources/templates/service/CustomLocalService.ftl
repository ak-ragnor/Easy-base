package ${entity.componentPackage("service")};

import ${entity.baseComponentPackage("service")}.${entity.name}BaseLocalService;

/**
* Local service interface for ${entity.name}.
* This interface extends the base service and can be customized.
*/
public interface ${entity.name}LocalService extends ${entity.name}BaseLocalService {
// CUSTOM CODE START: methods
// Add custom service methods here
// CUSTOM CODE END: methods
}