package ${entity.packageName}.model;

<#list entity.requiredImports as import>
import ${import};
</#list>
<#-- Import enums -->
<#list entity.fields as field>
<#if field.isEnum()>
import ${entity.packageName}.model.enums.${field.enumClass};
</#if>
</#list>

/**
* Interface defining the ${entity.entity} entity.
* Generated by EasyBase Code Generator.
*/
public interface ${entity.entity} {
    <#-- Property accessors -->
    <#list entity.fields as field>
    ${field.isEnum()?then(field.enumClass, field.javaType)} get${field.name?cap_first}();
    void set${field.name?cap_first}(${field.isEnum()?then(field.enumClass, field.javaType)} ${field.name});

    </#list>
    <#-- Business methods could be added here based on requirements -->
}