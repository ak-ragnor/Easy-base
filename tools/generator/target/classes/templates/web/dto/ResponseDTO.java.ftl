package ${entity.packageName}.web.dto;

import java.time.Instant;
<#list entity.requiredImports as import>
import ${import};
</#list>
<#-- Import enums -->
<#list entity.fields as field>
<#if field.isEnum()>
import ${entity.packageName}.model.enums.${field.enumClass};
</#if>
</#list>

/**
* DTO for outgoing ${entity.entity} responses.
* Generated by EasyBase Code Generator.
*/
public class ${entity.entity}ResponseDTO extends ${entity.entity}DTO {
<#-- Audit fields -->
<#if entity.hasAuditFields()>
<#-- Audit fields already exist in the base DTO -->
<#else>
private Instant createdDate;
private Instant lastModifiedDate;
</#if>

<#-- Getters and Setters for audit fields -->
<#if !entity.hasAuditFields()>
public Instant getCreatedDate() {
return createdDate;
}

public void setCreatedDate(Instant createdDate) {
this.createdDate = createdDate;
    }

    public Instant getLastModifiedDate() {
return lastModifiedDate;
}

    public void setLastModifiedDate(Instant lastModifiedDate) {
this.lastModifiedDate = lastModifiedDate;
}
    </#if>
}