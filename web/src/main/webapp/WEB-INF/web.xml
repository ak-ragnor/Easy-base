<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- Development Profile: Simplified Security -->
    <beans profile="dev">

        <!-- Very permissive security for development -->
        <security:http auto-config="true" use-expressions="true">
            <!-- Allow all health and info endpoints -->
            <security:intercept-url pattern="/api/health/**" access="permitAll"/>
            <security:intercept-url pattern="/api/info/**" access="permitAll"/>

            <!-- Allow all static resources -->
            <security:intercept-url pattern="/static/**" access="permitAll"/>
            <security:intercept-url pattern="/css/**" access="permitAll"/>
            <security:intercept-url pattern="/js/**" access="permitAll"/>
            <security:intercept-url pattern="/images/**" access="permitAll"/>
            <security:intercept-url pattern="/" access="permitAll"/>
            <security:intercept-url pattern="/index.html" access="permitAll"/>

            <!-- Allow all API endpoints for development -->
            <security:intercept-url pattern="/api/**" access="permitAll"/>

            <!-- Default allow all for dev -->
            <security:intercept-url pattern="/**" access="permitAll"/>

            <!-- Disable CSRF for REST APIs -->
            <security:csrf disabled="true"/>

            <!-- Enable CORS -->
            <security:cors/>

            <!-- Configure headers -->
            <security:headers>
                <security:frame-options policy="SAMEORIGIN"/>
                <security:content-type-options/>
            </security:headers>
        </security:http>

        <!-- Simple authentication manager (not used due to permitAll) -->
        <security:authentication-manager>
            <security:authentication-provider>
                <security:user-service>
                    <security:user name="dev" password="{noop}dev" authorities="ROLE_USER"/>
                </security:user-service>
            </security:authentication-provider>
        </security:authentication-manager>

    </beans>

    <!-- Production Profile: Secured -->
    <beans profile="prod,test">

        <security:global-method-security pre-post-annotations="enabled"/>

        <security:http auto-config="false" use-expressions="true">
            <!-- Public endpoints -->
            <security:intercept-url pattern="/api/public/**" access="permitAll"/>
            <security:intercept-url pattern="/api/auth/**" access="permitAll"/>
            <security:intercept-url pattern="/api/health" access="permitAll"/>
            <security:intercept-url pattern="/api/info" access="permitAll"/>

            <!-- Admin endpoints -->
            <security:intercept-url pattern="/api/admin/**" access="hasRole('ADMIN')"/>

            <!-- Protected API endpoints -->
            <security:intercept-url pattern="/api/**" access="isAuthenticated()"/>

            <!-- Static resources -->
            <security:intercept-url pattern="/static/**" access="permitAll"/>
            <security:intercept-url pattern="/" access="permitAll"/>

            <!-- Everything else requires authentication -->
            <security:intercept-url pattern="/**" access="isAuthenticated()"/>

            <security:csrf disabled="true"/>
            <security:cors/>

            <security:headers>
                <security:frame-options policy="DENY"/>
                <security:content-type-options/>
                <security:xss-protection/>
            </security:headers>

            <security:http-basic/>
        </security:http>

        <security:authentication-manager>
            <security:authentication-provider>
                <security:user-service>
                    <security:user name="admin" password="{bcrypt}$2a$12$XXXXXX" authorities="ROLE_ADMIN"/>
                    <security:user name="user" password="{bcrypt}$2a$12$YYYYYY" authorities="ROLE_USER"/>
                </security:user-service>
            </security:authentication-provider>
        </security:authentication-manager>

    </beans>

</beans>